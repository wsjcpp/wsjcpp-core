language: cpp

branches:
  only:
    - master

dist: bionic

addons:
  apt:
    packages:
    - cmake
    - make
    - g++
    - pkg-config

# Build steps
script:
  - mkdir -p tmp
  - cd tmp
  - make
  - cd ../unit-tests.wsjcpp
  - ./build_simple.sh
  - ./run_unit-tests.sh
